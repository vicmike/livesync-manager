apiVersion: v1
kind: Service
metadata:
  name: {{ include "livesync-manager.fullname" . }}
  labels:
    {{- include "livesync-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "livesync-manager.selectorLabels" . | nindent 4 }}
